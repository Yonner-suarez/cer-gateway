services:
  - type: web
    name: cer-gateway
    env: dotnet
    buildCommand: "dotnet publish -c Release -o ./publish"
    startCommand: "dotnet ./publish/CerGateway.dll"
    plan: free
    region: oregon
